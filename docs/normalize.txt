gfm -g -s ./wiki/ -d ./docs/

replace
 id="
 id="arrays-

 ">see also</a>
 " target="_blank">see also</a>

remove ul (menu)


run JavaScript:

console.clear();
console.log(`isDate
isError
isException
isNull
chainy`
  .split("\n")
  .reduce((carry, item) => {
 return carry + `
"${item}",`;
}, ""));